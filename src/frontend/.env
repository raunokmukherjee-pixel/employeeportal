# Local dev topology defaults for the Employee Internal Transfer frontend.
# Backend (src/backend/, Express, see implementation/src/index.js port convention) listens here:
BACKEND_PORT=3000
# Vite dev server listens here; /api/* is proxied to BACKEND_PORT (see vite.config.js).
FRONTEND_PORT=5173
